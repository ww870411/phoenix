时间：2026-03-19。问题：用户反馈每次后端代码更新并重部署容器后，虽然前端界面不一定立即显示退出，但很多需要登录的操作会报错。结论：后端持久化会话链路本身仍有效，remember_me=true 时 token 会保存在数据库并可在重启后由 AuthManager.require_session() 恢复；主要问题在前端两点：1）登录页默认未勾选记住我，很多用户实际使用的是仅内存/会话级登录；2）前端 API 层此前没有对通用 401 做统一收口，导致旧页面在后端重启后继续拿着失效 token 发请求，表现为“没自动登出，但业务操作报错”。实施：frontend/src/projects/daily_report_25_26/store/auth.js 将 rememberLogin 默认值从 false 改为 true，clearSession 后也恢复 true，login 默认 remember=true；新增 AUTH_EXPIRED_EVENT 监听，收到后清理本地会话、设置 lastError，并跳转 /login。frontend/src/projects/daily_report_25_26/services/api.js 新增 AUTH_EXPIRED_EVENT、notifyAuthExpired、authAwareFetch，统一在收到 401 时清理 token 并派发登录失效事件；登录接口显式 skipUnauthorizedHandling，避免错误密码时被当作会话过期。文档同步：configs/progress.md、frontend/README.md、backend/README.md。验证：2026-03-19 在 frontend 执行 npm run build 通过。留痕：本轮因 Serena 仅支持 Python 语义编辑，JS/Markdown 文件编辑降级使用 Desktop Commander 结构化编辑。