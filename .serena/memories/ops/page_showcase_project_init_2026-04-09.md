2026-04-09 新增 page_showcase（页面展示）项目。用户需求：在 /projects 末尾新增“页面展示”项目，仅 Global_admin 可见；backend_data/projects 下新增项目目录；前端进入后自动扫描项目目录中的顶层 HTML 文件并按文件名显示卡片，点击后打开对应 HTML 页面。实现摘要：1) 新增 backend/projects/page_showcase/api/router.py 与 workspace.py，提供 GET /api/v1/projects/page_showcase/page-showcase/pages 和 GET /api/v1/projects/page_showcase/page-showcase/html/{file_name}；2) 更新 backend/api/v1/project_router_registry.py 注册 page_showcase；3) 更新 backend_data/shared/项目列表.json 与 backend_data/shared/auth/permissions.json，仅向 Global_admin 开放；4) 新增 backend_data/projects/page_showcase/README.md 说明目录使用方式；5) 前端新增 frontend/src/projects/page_showcase/pages/PageShowcaseEntryView.vue 和 PageShowcaseViewerView.vue，并更新 ProjectSelectView.vue、ProjectEntryView.vue、router/index.js、daily_report_25_26/services/api.js。关键设计：后端认证仅支持 Authorization Bearer 头，不能直接把受保护 HTML 链接丢给浏览器打开，因此 viewer 方案采用前端带鉴权读取 HTML 文本，再通过 iframe srcdoc 在平台内预览；同时保留“新窗口打开”按钮，使用 blob URL 打开已拉取内容。降级说明：Serena 不支持对 .vue/.md 做可靠结构化编辑，因此代码文件与配置文件使用 apply_patch；README/progress 追加使用 desktop-commander write_file。验证结果：python py_compile 通过（page_showcase workspace/router 与 project_router_registry），frontend npm run build 通过。风险提示：当前仅适合独立单文件 HTML；若 HTML 依赖同目录外链资源，需后续补充资源代理或静态资源映射能力。