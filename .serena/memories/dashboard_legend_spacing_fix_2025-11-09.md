# 仪表盘图例下移与网格留白调整（2025-11-09）
- 背景：多张图表的图例位于图表底部，遮挡了 x 轴刻度文本。
- 操作：在 `frontend/src/daily_report_25_26/pages/DashBoard.vue` 中统一给各图表设置 `legend.bottom`，并相应增大 `grid.bottom` 留白，保证图例位于坐标轴下方；同时将气温卡片取消满宽修饰，使其与边际利润卡片并排显示。
- 影响：仪表盘上所有图例与坐标轴文字互不遮挡，顶部两张卡片保持等高且平均分布。
- 回滚：恢复 `DashBoard.vue` 至本次修改前版本即可撤销图例与网格调整。