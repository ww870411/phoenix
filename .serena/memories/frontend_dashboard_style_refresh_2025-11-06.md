## 2025-11-06 仪表盘样式升级
- 对 `DashBoard.vue` 的摘要卡片与表格视觉做大幅美化：新增渐变主题卡片、图标掩码、玻璃拟态阴影以及规范化表格样式。
- `DashboardTable` 支持空数据占位，且表头使用渐变背景；保留静态示例数据结构。
- CDN 方式引入 `echarts`，通过本地 `EChart` 组件管理实例，无需额外 npm 依赖。
- 回滚：删除新增样式与 `EChart` 组件，恢复旧版模板即可。