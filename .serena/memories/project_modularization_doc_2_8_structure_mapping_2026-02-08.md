# 2026-02-08 项目模块化文档沉淀（2.8）

## 用户需求
在 `configs` 下新增 `2.8项目模块化.md`，清晰写出原先文件结构、新结构及对应关系。

## 执行内容
1. 扫描现状目录：`backend`、`frontend/src`、`backend_data`、`configs`。
2. 新建文档：`configs/2.8项目模块化.md`。
3. 文档内容包含：
- 改造目标与范围；
- 改造前结构树（后端/前端/数据目录）；
- 当前结构树（后端/前端/数据目录）；
- 旧路径->新路径映射表（后端代码、前端页面、backend_data）；
- 兼容策略（新目录优先+旧路径回退、shim 转发、URL 保持）；
- 新项目落位规则与完成度结论。
4. 同步留痕：
- `configs/progress.md` 新增本次记录；
- `backend/README.md`、`frontend/README.md` 增加该文档索引。

## 结果
形成可直接指导后续项目接入与排障的结构对照文档，降低多人协作时对“文件应放哪里”的歧义。