## 2025-11-08 气温图业务日标记
- 背景：仪表盘气温折线图需要强调当前业务日（push_date），便于在滑动窗口中快速定位。
- 变更：DashBoard.vue 在生成 ECharts option 时，若当前数据集中包含 push_date，自动为“当期”折线添加 markLine（虚线+标签“业务日期”）。
- 效果：打开仪表盘或切换 show_date 时，气温折线会在对应日期列出现标注，帮助用户识别当前业务日；若数据中不包含该日期则自动隐藏标记。