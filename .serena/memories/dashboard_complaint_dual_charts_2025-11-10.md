# 投诉卡片双图拆分（2025-11-10）
- 背景：原内嵌柱状展示效果不符合预期，改为分别展示“省市平台服务投诉量”“净投诉量”两张图，仍需体现本期/同期对比并保留明细表；同时同步优化收入卡片呈现。
- 改动：
  - `frontend/src/daily_report_25_26/pages/DashBoard.vue`：移除 `useComplaintsOption`，新增 `useComplaintSingleOption` 与 `complaintChartConfigs`，模板中按数组渲染双柱图并共享表格；同期系列改为橙色柱形（取消斜纹），图表高度 260px。
  - 栅格与样式：投诉卡片在 ≥1024px 占 8 列、最小高度 360px，收入卡片缩为 4 列；新增 `complaint-charts*` 样式控制双列排布与标题。
  - 收入卡片新增表格（共享 Table 组件）展示分类、本期、同期数值，复用 `formatIncomeValue` 保持千分位与两位小数。
- 影响：投诉卡片现在显示两张本期/同期对比柱状图与明细表；收入卡片同时具备图表与数据表；后端 `/dashboard` 输出结构未改动。
- 验证：刷新仪表盘应看到两张投诉图（蓝/橙对比）与表格同卡片展示，收入卡片新增表格同步更新；在 ≥1024px 下保持 8+4 列布局。