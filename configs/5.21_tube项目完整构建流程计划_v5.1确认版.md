# 保温管物流链项目完整构建流程计划（V5.1确认版）

日期：2026-05-21  
适用项目：`insulation_pipe_supply_2026`  
依据文档：`configs/5.21_tube项目建设方案_v5.1_物流链管理版.md`  
适用对象：Codex / Phoenix项目开发执行 / 项目方案复核

---

## 1. 文档目的

本文件用于在原《保温管物流链项目完整构建流程计划》基础上，根据V5.1方案确认意见，更新tube项目的一阶段开发蓝图。

本文件重点明确：

1. tube项目在Phoenix平台中的接入方式不变；
2. 四个项目入口key正式调整；
3. 施工单位账号与换热站映射关系纳入配置；
4. 库管权限、撤销规则、禁止回退规则、历史数据修改规则正式冻结；
5. 页面开发顺序、dashboard定位、库存调整表策略按V5.1执行。

本计划默认目标仍为：**在Phoenix平台现有技术底座内，独立落地一套可运行、可验证、可交付的一阶段保温管物流链管理子系统。**

---

## 2. Phoenix平台背景说明

Phoenix当前不是单页面小工具，也不是为tube项目临时搭建的新站点，而是一个已经运行中的平台级Web应用。tube项目应作为Phoenix中的新业务子系统接入。

### 2.1 Phoenix当前整体形态

Phoenix为“统一登录 + 项目选择 + 多项目模块挂载”的平台式应用：

1. 用户先登录平台；
2. 登录后进入项目选择页；
3. 根据账号权限进入不同项目空间；
4. 各项目在同一平台壳层下运行，但拥有独立页面、接口、数据配置与业务逻辑；
5. 平台统一提供认证、权限、路由壳层、公共组件、部署体系与部分通用服务。

### 2.2 前端基本情况

Phoenix前端基于：

1. `Vue 3`
2. `Pinia`
3. `Vue Router`
4. `Vite`

前端根入口：

```text
frontend/src/main.js
```

全局路由入口：

```text
frontend/src/router/index.js
```

项目页面组织方式：

```text
frontend/src/pages/
frontend/src/projects/
```

新tube项目前端目录建议为：

```text
frontend/src/projects/insulation_pipe_supply_2026/
```

### 2.3 后端基本情况

Phoenix后端基于：

1. `FastAPI`
2. 统一 `/api/v1` 路由前缀
3. 项目级路由注册机制

后端根入口：

```text
backend/main.py
```

总路由入口：

```text
backend/api/v1/routes.py
```

项目路由注册表：

```text
backend/api/v1/project_router_registry.py
```

tube项目后端目录建议为：

```text
backend/projects/insulation_pipe_supply_2026/
```

统一API前缀建议为：

```text
/api/v1/projects/insulation_pipe_supply_2026/...
```

### 2.4 数据与配置目录

建议保留以下项目数据目录：

```text
backend_data/projects/insulation_pipe_supply_2026/
```

其中放置：

1. `tube_config.json`
2. 测试数据或初始化数据；
3. 项目相关导入导出文件；
4. 后续必要的文档或运行配置。

---

## 3. V5.1确认后的项目入口key

V5.1正式确认四个项目入口key如下：

| 入口key | 页面名称 | 核心职责 | 首版定位 |
|---|---|---|---|
| `dashboard` | 全局数据看板 | 全局汇总、便利图形展示、关键指标、风险辅助提示 | 只查看，不操作 |
| `supply_management` | 供给侧管理 | 查看需求、缺口、发货登记、本供给主体发货进度跟踪 | 供给主体主操作入口 |
| `demand_management` | 需求侧管理 | 未来三日计划、到货确认、每日实际使用填报、需求侧库存视图 | 现场负责人主操作入口，优先开发 |
| `warehouse_management` | 库管管理 | 库管台账、库管确认、手续状态留痕 | 库管人员主操作入口 |

### 3.1 命名执行要求

1. 不再使用旧key：`raw_materials`、`production_allocation`、`demand`。
2. 项目清单、前端路由、页面组件、后端API分组，应统一迁移至新key。
3. 配置key建议使用下划线形式，例如：`supply_management`。
4. URL路径可使用短横线形式，例如：`supply-management`。
5. 页面组件可使用大驼峰形式，例如：`SupplyManagement.vue`。

### 3.2 推荐目录示例

```text
frontend/src/projects/insulation_pipe_supply_2026/pages/Dashboard.vue
frontend/src/projects/insulation_pipe_supply_2026/pages/SupplyManagement.vue
frontend/src/projects/insulation_pipe_supply_2026/pages/DemandManagement.vue
frontend/src/projects/insulation_pipe_supply_2026/pages/WarehouseManagement.vue
```

---

## 4. 总体实施原则

1. 先跑通主流程，再补强体验层。
2. 先固化数据、权限和状态机，再做页面视觉。
3. 先做一阶段闭环，不把二期能力混入首版。
4. 能复用Phoenix现有能力的地方不重复造轮子。
5. 入口key、状态、权限边界、撤销与回退规则按V5.1冻结口径执行。
6. dashboard优先做汇总与图形展示，警示为辅，只查看不操作。

---

## 5. 首版交付边界

本轮构建覆盖V5.1方案中的第一阶段建设范围，不主动纳入以下内容：

1. 运输车辆、司机、路线管理；
2. 电子签名、图片签收、定位打卡；
3. 复杂审批流；
4. 正式确认文件自动生成；
5. 完整退库建议与调剂闭环；
6. ERP / OA / 财务系统深度集成；
7. 独立异常闭环系统；
8. 独立库存快照系统；
9. 智能调度算法；
10. 施工单位“不同意接收”及争议闭环；
11. 到货确认、施工接收、库管确认后的反向回退；
12. 历史计划、历史实际使用记录的受控纠错机制。

---

## 6. 配置层落地方案

### 6.1 `tube_config.json`建议结构

V5.1确认配置文件中应包含施工单位配置及施工单位与换热站映射关系。

```json
{
  "supply_entities": [],
  "demand_entities": [],
  "pipe_models": [],
  "manager_assignments": [],
  "construction_units": [],
  "construction_assignments": []
}
```

### 6.2 配置项说明

| 配置项 | 内容 | 说明 |
|---|---|---|
| `supply_entities` | 供给主体 / 管厂 | 记录供给单位名称、简称、联系人、联系电话、状态等。 |
| `demand_entities` | 换热站 / 需求主体 | 记录换热站名称、区域、标段、施工状态等。 |
| `pipe_models` | 保温管型号字典 | 记录型号、管径、单位、分类等。 |
| `manager_assignments` | 现场负责人绑定关系 | 记录规划发展部现场负责人与换热站的绑定关系。 |
| `construction_units` | 施工单位配置 | 记录施工单位名称、联系人、账号关联、状态等。 |
| `construction_assignments` | 施工单位与换热站映射关系 | 记录施工单位与需求主体之间的独立对应关系。 |

### 6.3 配置字段预留原则

1. 换热站配置中应预留区域字段。
2. 换热站配置中应预留标段字段。
3. 换热站配置中应预留施工状态字段，如未开工、施工中、已完工。
4. 这些字段第一阶段可以先用于筛选或隐藏备用，不必全部做成复杂页面功能。
5. 后续看板、权限、筛选和复盘需要时，可逐步启用。

### 6.4 映射关系特别说明

1. 一个现场负责人可以绑定多个换热站。
2. 一个施工单位可以绑定多个换热站。
3. 施工单位与换热站的映射关系不必与现场负责人映射关系一致。
4. 现场负责人负责计划、到货确认、实际使用填报。
5. 施工单位负责其绑定换热站的施工接收确认。

---

## 7. 数据库核心表建设

### 7.1 第一阶段核心业务表

| 序号 | 表名 | 作用 | V5.1处理意见 |
|---|---|---|---|
| 1 | `tube_baseline_quantity` | 设计用量、计划采购量 | 第一阶段建设，作为核心业务基准数据。 |
| 2 | `tube_daily_plan` | 每日、每站、每型号计划量 | 第一阶段建设，长表保存，不只保存未来三天。 |
| 3 | `tube_delivery` | 发货、到货、施工接收、库管确认生命周期主表 | 第一阶段建设，作为物流链状态主表。 |
| 4 | `tube_daily_usage` | 每日、每站、每型号实际使用量 | 第一阶段建设，长表保存。 |
| 5 | `tube_inventory_adjustment` | 库存调整、盘点差异、退库、调剂、破损、纠错 | 第一阶段建设表结构，页面弱化。 |

### 7.2 不单独建设的表

第一阶段不建设以下独立表：

1. `tube_user_permission`：沿用Phoenix平台账户和权限文件；
2. `tube_delivery_confirmation`：合并进入`tube_delivery`生命周期主表；
3. `tube_exception_record`：第一阶段不做复杂异常闭环；
4. 库存余额快照表：第一阶段实时计算库存余额，不双写库存。

### 7.3 `tube_delivery`状态机

V5.1确认状态如下：

```text
已发货待到货
    ├─ 撤销 → 已撤销（仅限到货确认前）
    ↓
已到货待接收
    ↓
已接收待库管确认
    ↓
已完成
```

异常可通过字段标记：

```text
abnormal_flag = true
remark / arrived_remark / received_remark / warehouse_remark 记录原因
```

---

## 8. 权限与角色约束

### 8.1 角色清单

| 角色 | 查看范围 | 操作范围 |
|---|---|---|
| `global_admin` | 全部数据 | 配置维护、基准数据调整、特殊系统管理。 |
| `site_manager` | 绑定换热站数据 | 未来三日计划、到货确认、实际使用填报。 |
| `construction_unit` | 映射换热站相关接收事项 | 施工接收确认。 |
| `supplier` | 全部需求、库存、缺口 | 仅操作本供给主体发货记录。 |
| `warehouse_keeper` | 库管台账相关流水 | 仅库管确认。 |
| `viewer` | 授权范围内看板或台账 | 只查看。 |

### 8.2 库管人员限制

V5.1确认库管人员第一阶段：

1. 可以查看库管台账；
2. 可以执行库管确认；
3. 不可以修改发货记录；
4. 不可以修改到货确认；
5. 不可以修改施工接收；
6. 不可以修改未来三日计划；
7. 不可以修改实际使用量；
8. 不可以修改基准数据；
9. “不同意”或争议处理留到第二阶段。

### 8.3 施工单位限制

1. 施工单位通过`construction_assignments`确定换热站范围。
2. 施工单位只确认其映射范围内换热站的接收事项。
3. 施工单位不得确认到货。
4. 施工单位不得填报计划。
5. 施工单位不得填报实际使用量。
6. 施工单位不得操作库管确认。

---

## 9. 撤销、回退与历史修改规则

### 9.1 发货撤销规则

V5.1确认：

1. 发货记录仅在“已发货待到货”状态下允许撤销。
2. 撤销不等于删除，必须保留原始发货记录。
3. 撤销时必须记录撤销人、撤销时间和撤销原因。
4. 撤销后状态应变更为“已撤销”。
5. 一旦进入“已到货待接收”及后续状态，第一阶段禁止撤销。

### 9.2 确认后禁止回退

V5.1确认第一阶段禁止以下反向回退：

1. 到货确认后的回退；
2. 施工接收确认后的回退；
3. 库管确认后的回退；
4. 已完成状态回退到前置状态。

如发生异常，第一阶段通过备注、异常标记、库存调整预留表或线下处理；正式异常闭环留到第二阶段。

### 9.3 历史计划和历史使用记录不允许修改

V5.1确认：

1. 历史计划记录第一阶段不允许修改。
2. 历史实际使用记录第一阶段不允许修改。
3. 相关纠错机制留到第二阶段设计。
4. 第一阶段如发现问题，可先通过备注、异常说明、线下核实方式处理。

---

## 10. 库存与缺口计算规则

### 10.1 库存实时计算

第一阶段不建设库存余额表，库存按业务数据实时计算：

```text
当前现场库存 = 累计施工接收量 - 累计实际使用量 ± 库存调整量
```

### 10.2 未来三日缺口

```text
未来三日计划量 = 当日计划量 + 次日计划量 + 第三日计划量

现场硬缺口 = max(未来三日计划量 - 当前现场库存量, 0)

净待发缺口 = max(未来三日计划量 - 当前现场库存量 - 已发货待到货量 - 已到货待接收量, 0)
```

V5.1确认：净待发缺口默认扣减“已到货待接收量”。但该数量仍需单独展示，不能混同为已入账库存。

---

## 11. 前端页面开发顺序

V5.1确认页面开发顺序如下：

```text
1. demand_management
2. supply_management
3. warehouse_management
4. dashboard
```

### 11.1 第一优先：`demand_management`

原因：需求侧是业务数据源头。

首版能力：

1. 未来三日计划填报；
2. 到货确认；
3. 每日实际使用填报；
4. 本换热站库存与缺口查看；
5. 现场负责人“我的换热站”默认过滤视图。

### 11.2 第二优先：`supply_management`

首版能力：

1. 需求汇总；
2. 供给侧缺口查看；
3. 发货登记；
4. 本供给主体发货进度追踪；
5. “已发货待到货”状态下发货撤销；
6. 超时未确认提醒。

### 11.3 第三优先：`warehouse_management`

首版能力：

1. 库管台账；
2. 状态筛选；
3. 库管确认；
4. 手续留痕备注；
5. 只看、只确认，不做其他修改。

### 11.4 第四优先：`dashboard`

V5.1确认dashboard定位：

1. 汇总优先；
2. 图形便利；
3. 警示为辅；
4. 只查看，不操作。

首版能力：

1. 全局指标卡；
2. 简洁图形展示；
3. 需求侧聚合台账；
4. 供给侧聚合台账；
5. 库管状态聚合台账；
6. 风险预警辅助区；
7. 可跳转明细，但不直接执行发货、确认、修改等操作。

---

## 12. 主流程API建议

第一批API建议覆盖：

1. 配置读取接口；
2. 基准数据录入/查询接口；
3. 未来三日计划录入/查询接口；
4. 供给主体发货接口；
5. 发货撤销接口，仅允许已发货待到货状态；
6. 到货确认接口；
7. 施工接收确认接口；
8. 库管确认接口；
9. 实际使用填报接口；
10. 库存、缺口、风险聚合接口；
11. 台账查询接口；
12. dashboard汇总接口。

最优先跑通链路：

```text
配置读取
-> 基准数据
-> 计划填报
-> 发货登记
-> 到货确认
-> 施工接收
-> 库管确认
-> 实际使用
-> 库存与缺口计算
-> dashboard汇总展示
```

---

## 13. 完整实施阶段

| 阶段 | 工作重点 | 主要内容 | 阶段成果 |
|---|---|---|---|
| 阶段0 | 方案冻结与开发蓝图化 | 固化V5.1规则、入口key、权限矩阵、状态机、字段和接口清单。 | 开发蓝图。 |
| 阶段1 | 项目骨架挂载 | 建前后端目录，更新项目清单，挂载四个新入口，注册后端路由。 | 项目可从Phoenix进入。 |
| 阶段2 | 配置层落地 | 建`tube_config.json`，包含供给主体、换热站、型号、现场负责人、施工单位、施工单位映射。 | 配置读取服务。 |
| 阶段3 | 数据库核心表 | 建五张核心/预留表：baseline、plan、delivery、usage、inventory_adjustment。 | 表结构和测试数据能力。 |
| 阶段4 | 后端领域服务 | 配置、基准、计划、发货生命周期、使用、库存计算、权限校验、看板聚合。 | 服务层能力。 |
| 阶段5 | 主流程API | 打通计划、发货、撤销、到货、施工接收、库管确认、使用、库存计算。 | 可驱动前端的API。 |
| 阶段6 | `demand_management`页面 | 计划、到货确认、实际使用、需求侧库存视图。 | 需求侧可运行。 |
| 阶段7 | `supply_management`页面 | 需求查看、发货登记、发货进度、撤销。 | 供给侧可运行。 |
| 阶段8 | `warehouse_management`页面 | 库管台账、库管确认、手续留痕。 | 库管侧可运行。 |
| 阶段9 | `dashboard`页面 | 汇总卡片、便利图形、台账汇总、风险辅助。 | 管理展示可用。 |
| 阶段10 | 权限接入与边界校验 | 角色、scope、动作权限、前后端双层校验。 | 权限闭环。 |
| 阶段11 | 计算预警与验证 | 库存、缺口、超时提醒、状态流转、数量边界测试。 | 验证记录和问题清单。 |
| 阶段12 | 试运行与收尾 | 小范围试运行、收集问题、优化文档。 | 试运行版本。 |

---

## 14. 给Codex的执行注意事项

1. 以V5.1方案为准，不再以V5.0中的旧入口key为准。
2. 项目入口key必须使用：`dashboard`、`supply_management`、`demand_management`、`warehouse_management`。
3. `tube_config.json`必须增加`construction_units`和`construction_assignments`。
4. 施工单位映射关系独立于现场负责人映射关系。
5. 库管人员只能查看库管台账并执行库管确认。
6. 第一阶段建设`tube_inventory_adjustment`表结构，但不要扩展复杂库存调整页面。
7. 发货撤销只允许在“已发货待到货”状态下进行。
8. 撤销必须留痕，不物理删除。
9. 到货确认、施工接收、库管确认后禁止回退。
10. 历史计划和历史实际使用记录第一阶段不允许修改。
11. 净待发缺口默认扣减已发货待到货量和已到货待接收量。
12. dashboard汇总优先、图形便利、警示为辅，且只查看不操作。
13. 前端页面按`demand_management` → `supply_management` → `warehouse_management` → `dashboard`顺序推进。
14. 主流程和权限边界优先于视觉美化。
15. 所有关键操作必须记录操作人、操作时间和必要备注。

---

## 15. 当前已拍板事项汇总

| 序号 | 事项 | 确认意见 |
|---|---|---|
| 1 | 四个入口key | `dashboard`、`supply_management`、`demand_management`、`warehouse_management`。 |
| 2 | 施工单位账号 | 设置独立账号。 |
| 3 | 施工单位映射 | 单独配置施工单位与换热站映射。 |
| 4 | 施工单位映射是否等同现场负责人映射 | 不等同，单独维护。 |
| 5 | 库管人员权限 | 只看、只确认，不修改。 |
| 6 | 配置字段 | 区域、标段、施工状态先预留。 |
| 7 | 库存调整表 | 第一阶段建设表结构，页面弱化。 |
| 8 | 发货撤销 | 仅“已发货待到货”状态允许撤销。 |
| 9 | 确认后回退 | 第一阶段禁止。 |
| 10 | 历史计划/历史使用修改 | 第一阶段不允许。 |
| 11 | 净待发缺口 | 默认扣减已到货待接收量。 |
| 12 | dashboard展示 | 汇总优先、图形便利、警示为辅。 |
| 13 | dashboard操作 | 只查看，不操作。 |
| 14 | 前端开发顺序 | 需求侧、供给侧、库管侧、全局看板。 |

---

## 16. 最后判断

基于V5.1确认意见，tube项目已经具备进入正式开发实施的条件。当前阶段不需要继续扩大需求范围，最重要的是按照已确认的入口、配置、表结构、状态机、权限边界和页面顺序推进。

后续开发中，若出现未覆盖的异常场景，应优先记录为二期问题，不应打断第一阶段主流程落地。

---

## 17. 当前实施进度（截至2026-05-22）

以下内容用于记录围绕本计划已经实际落地的事项。  
记录原则：**只保留最终状态，不重复记录中途修正过程。**

### 17.1 项目接入与基础骨架

1. `insulation_pipe_supply_2026` 已正式接入 Phoenix 平台，不再是仅存在于方案中的待建项目。
2. 当前项目页面入口已固定为：
   - `dashboard`
   - `global_management`
   - `supply_management`
   - `demand_management`
   - `warehouse_management`
3. 后端已完成项目级路由注册，可通过 `/api/v1/projects/insulation_pipe_supply_2026/...` 提供接口。
4. tube 项目已纳入平台统一登录、项目选择、页面选择与权限过滤链路。

### 17.2 配置层与演示基础数据

1. `backend_data/projects/insulation_pipe_supply_2026/tube_config.json` 已正式落地并投入使用。
2. 当前配置已稳定维护以下核心内容：
   - `biz_date`
   - `plan_start_date`
   - `plan_editable_days`
   - `supply_entities`
   - `demand_entities`
   - `pipe_models`
   - `production_capacities`
   - `manager_assignments`
   - `construction_units`
   - `baseline_presets`
3. 配置结构已完成当前阶段收口：
   - 保温管型号统一使用大写 `DNxx`
   - 已删除型号中的“口径标签”“分类”“状态”
   - 供给主体不再保留“简称”
   - 现场负责人映射不再保留“换热站名称列表”
   - 施工单位与施工单位映射已合并为单一区块，并通过 `station_ids` 建立映射
4. 演示基础数据已完整预置：
   - 2 个供给主体
   - 4 个换热站
   - 2 个现场负责人
   - 2 个施工单位
   - 10 个演示保温管型号
5. `baseline_presets` 已完成 4 个换热站 × 10 个型号的非零覆盖，且已按“小口径偏多、中口径偏少、大口径偏多”的演示规则分布。
6. `production_capacities` 已按“约 30 天完成设计量供给”的口径预填，并按供给主体分工配置。

### 17.3 数据库与初始化脚本

1. 已在 `backend/sql/` 下生成统一建表脚本。
2. 当前 tube 项目使用独立 schema：
   - `tube`
3. 当前核心表已全部落地：
   - `tube.tube_baseline_quantity`
   - `tube.tube_daily_plan`
   - `tube.tube_delivery`
   - `tube.tube_daily_usage`
   - `tube.tube_inventory_adjustment`
4. 当前数据库已按该脚本完成建表，后续接口已开始实际读写其中部分表。

### 17.4 权限、用户与可见性

1. 已在共享鉴权文件中加入 tube 项目测试用户、分组与项目可见性配置。
2. 当前角色体系已明确为：
   - `Global_admin`
   - `tube_supplier`
   - `tube_site_manager`
   - `tube_construction_unit`
   - `tube_warehouse_keeper`
3. `Global_admin` 已具备 tube 项目的最高权限，可查看并操作全部项目数据，并进入“全局管理入口”维护配置。
4. tube 项目已完成“项目入口可见性 + 页面访问 + 基础动作权限”的首轮接通。

### 17.5 全局管理入口

1. 已新增 `global_management` 页面，展示名称已统一为“全局管理入口”。
2. 当前页面已具备结构化配置维护能力，不再依赖手改整份 JSON。
3. 当前可在页面中直接维护：
   - `biz_date`
   - `plan_start_date`
   - `plan_editable_days`
   - 供给主体
   - 换热站
   - 型号
   - 现场负责人映射
   - 施工单位及换热站映射
   - 基准量预设
   - 保温管生产能力
4. 各主要分块已支持独立保存。
5. 型号已支持页面内新增与删除。
6. 基准量预设已改为按换热站过滤编辑，而非一次性展开全部换热站。
7. 页面底部保留原始 JSON 折叠查看区，仅用于核对，不作为主要维护方式。

### 17.6 需求侧管理页面（`demand_management`）

1. `demand_management` 已从占位页升级为真实工作台页面，并已统一到 tube 项目公共壳层。
2. 当前页面已接通以下真实能力：
   - 换热站切换
   - 基准量台账查看
   - 未来三日计划查询与提交
   - 实际使用量查询与提交
   - 物流确认记录查看
   - 到货确认
   - 施工接收确认
3. 当前页面已正确接入数据库与配置回退逻辑：
   - 三日计划读写 `tube_daily_plan`
   - 实际使用读写 `tube_daily_usage`
   - 物流确认记录读取 `tube_delivery`
   - 基准量优先读取 `tube_baseline_quantity`，无数据时回退到 `baseline_presets`
4. 页面日期语义已稳定拆分为：
   - `biz_date`：实际发生量日期
   - `plan_start_date`：三日计划起始日期
5. 页面当前只读展示全局日期，不再在需求页直接修改全局配置。
6. 页面已接入 `plan_editable_days` 规则：
   - `3`：三天都可编辑
   - `2`：仅最后两天可编辑
   - `1`：仅最后一天可编辑
   - `0`：三天都不可编辑
7. 物流确认按钮当前最终规则如下：
   - 所有可访问该页面的角色都能看到“确认到货 / 施工接收”两个按钮
   - 仅当前角色且当前流程步骤允许时，按钮显示深色并可点击
   - 其余情况显示为白底、浅灰边、浅灰字且不可点击
8. 当前角色与按钮映射已收口为：
   - `tube_site_manager` 在 `pending_arrival` 状态可执行到货确认
   - `tube_construction_unit` 在 `pending_receive` 状态可执行施工接收
   - `Global_admin` 可在需求页按当前状态执行前述两类确认
9. 当前页面已修复关键联调问题，包括：
   - 计划回显与保存字段映射
   - 选项字段兼容
   - 物流记录字段兼容
   - `deliveryId` 归一化缺失导致的“按钮可见但无响应”问题

### 17.7 供给侧管理页面（`supply_management`）

1. `supply_management` 已从占位页升级为真实工作台页面，并已统一到 tube 项目公共壳层。
2. 当前页面已接通以下真实能力：
   - 保温管供需明细查看
   - 视图模式切换
   - 型号多选筛选
   - 发货登记
   - 发货记录列表查看
   - 发货撤销
3. 当前供给侧后端接口已稳定接入：
   - `GET /supply-management/options`
   - `GET /supply-management/demand-summary`
   - `GET /supply-management/deliveries`
   - `POST /supply-management/deliveries`
   - `POST /supply-management/deliveries/{delivery_id}/cancel`
4. 当前供给侧汇总口径已收口为：
   - 原“当前缺口”已改为 `三日净缺口`
   - `三日净缺口 = 三日计划 - 所有在途 - 当前库存`
   - 当前库存按 `总到货 - 总使用量` 实时计算
5. 发货单号已统一为短格式：
   - `前缀-YYMMDD-序号`
   - 例如 `SA-260522-001`
6. 页面主体选择逻辑已按角色收口：
   - 普通供给主体账号在“保温管供需明细”和“发货登记”中都只读显示自己的主体
   - `Global_admin` 在发货登记中仍可切换主体
7. 发货撤销规则已按 V5.1 执行：
   - 仅 `pending_arrival` 状态允许撤销
   - 撤销不物理删除，而是改为 `cancelled`
   - 记录撤销人、撤销时间与撤销原因

### 17.8 库管管理页面（`warehouse_management`）

1. `warehouse_management` 已从占位页升级为可操作台账页。
2. 当前页面职责已收口为：
   - 展示物流链前序状态
   - 仅在 `pending_warehouse` 状态下提供库管确认入口
3. 前序状态在库管页中仅可见，不再提供到货确认和施工接收的替代性操作按钮。
4. 当前该页面已符合“库管只做手续闭环确认”的设计目标。

### 17.9 当前总体结论

1. 截至 2026-05-22，tube 项目已经完成以下关键阶段：
   - 项目接入
   - 配置层落地与结构收口
   - 数据库建表
   - 测试权限体系与角色接入
   - 全局管理入口
   - 需求侧真实工作台
   - 供给侧真实工作台
   - 库管页第一版闭环入口
2. 当前主流程已打通到：
   - 计划填报
   - 发货登记
   - 到货确认
   - 施工接收
   - 库管确认
3. 当前阶段的工作重点已经从“方案与配置收口”转入“全链路联调与展示层完善”。

### 17.10 当前尚未完成部分

1. `dashboard` 的正式汇总卡片、便利图形与风险辅助展示。
2. 全链路异常分支、边界条件与角色动作的进一步回归验证。
3. 库存、缺口、超时提醒等聚合计算的系统性联调与校验。
