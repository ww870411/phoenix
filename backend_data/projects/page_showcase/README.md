# 页面展示项目目录

本目录用于存放“页面展示”项目要展示的静态 HTML 页面。

使用方式：

1. 将独立可运行的 `.html` 或 `.htm` 文件直接放在当前目录下。
2. 前端项目页会自动扫描当前目录中的页面文件，并按文件名生成卡片。
3. 卡片标题与文件名保持一致，点击后会在平台内打开对应页面。

注意：

- 当前版本仅扫描当前目录下的顶层 HTML 文件，不递归子目录。
- 为避免鉴权和资源路径问题，建议使用单文件静态页面，或将依赖资源内联到 HTML 中。
